## PROOF OF READING

1. `README.md` (~line 18): "Attribution scores become training labels for a ranking model that predicts, per candidate profile, the incremental retention value of showing it."
2. `discovery/D3_retention_attribution.md` (~line 17): "Published **strict** per-touch retention attribution (identified causal credit of N-day active to one swipe/match/rec item amid many exposures) was **not found** in industry or dating-specific literature."
3. `review/context/q2-class-index.md` (line 1): "# Q2 class index — NLM card labels (re-classify with D3 rubric if they conflict)"
4. `review/context/compact-summaries-1.md` (~line 377): "**Core Problem:** Traditional Rule-Based Attribution (RBA)—such as last-touch or last-click—severely overvalues bottom-of-funnel channels (e.g., Search, Email) while completely ignoring upper and mid-funnel touchpoints (e.g., Video Ads, Digital Display) [4, 5]."
5. `review/context/compact-summaries-2.md` (~line 97): "**Q1 (Industry/Production Attribution 2025–2026):** **Yes.** Published in May 2026 (`arXiv:2605.08881`), this paper describes a production causal Multi-Touch Attribution (MTA) system deployed at Kuaishou Technology on a recommendation engine serving **400 million Daily Active Users (DAU)** and evaluated over **30 billion training samples** [1-3]."
6. `review/context/nlm-synthesis-q3-last-touch-replacement.md` (~line 111): "**None of the scoped papers estimate the true incremental effect of removing one swipe (or one item) on binary N7 retention.**"
7. `method-tracker.md` (~line 53): "Until then, its deletion score must be called a model-based removal effect, not incremental N7 lift."
8. `literature-review.md` (~line 57): "The card overrides the looser D3 discovery label: because these scores update an RL policy and are not reusable causal retention credits, classification: NOT."
9. `executive-summary.md` (~line 102): "Liu et al., *ALM-MTA: Front-Door Causal Multi-Touch Attribution Method for Creator-Ecosystem Optimization*, ICLR 2026 supports removal-effect labels, but no card validates that contrast on binary N7."

---

## VERDICT: PASS WITH FIXES

The load-bearing parts hold up. Citation discipline is clean — I found no citations to Amazon Ads MTA, the LiDDA arXiv paper, MAL, or CABB anywhere in the three deliverables; LinkedIn is cited correctly as the in-scope engineering blog. Incremental language is correctly constrained in all three files (v1 is explicitly named a "removal score, not incremental lift", and the plan verdict repeats the prohibition), which matches the NLM q3 flag. GFN4Retention and MRet are both classified NOT with an explicit correction of the related-notebook over-classification. The fixes below are classification conflicts, one missing verdict component, and traceability gaps — not a rewrite.

### Fixes

1. **`executive-summary.md`, "Against the 2026-06-13 plan" (~lines 101–105) — no verdict on the binary-N7 component.** The section gives keep/change verdicts on v1 and v2 and adds v3, but never states a verdict on the plan's first decision: binary next-7-day retention as the target label. Add an explicit "keep binary N7" bullet, sourced to the fact that the closest per-item templates use a shorter binary horizon (Ding et al., *Rethinking User Retention Modeling in Recommendation*, TOIS 2026 — next-3-day retention, per compact-summaries-2) while the only long-horizon alternative in scope is a 60-day progressive active-day trace (Zhang et al., *Impatient Bandits*, JMLR — compact-summaries-1), and note that per the NLM q3 flag no card validates per-touch credit on binary N7 at all.

2. **All three files — ALM-MTA's venue "ICLR 2026" is not supported by any card.** The compact summary gives only "Published in May 2026 (`arXiv:2605.08881`)" with no venue; "ICLR" appears only in the card *filename*, which is a run artifact. This sits in the first sentence of the Q1 answer, in the method-tracker rank-1 row, and in the recommendation citations. Change to "arXiv 2026; Kuaishou" throughout unless a card supports the venue. (MRet's "ICLR 2026" is fine — compact-summaries-2 says "Submitted to **ICLR 2026**" and D3 §B1 uses it.)

3. **`literature-review.md` (~line 57) and `executive-summary.md` G4 — the FID demotion contradicts D3 and is justified by a card header the run itself treats as unreliable.** D3 §A3 classifies FID **PARTIAL** and §H.2 names it one of the three "best partial hits"; the deliverables classify it NOT. The stated reason, "the card overrides the looser D3 discovery label," inverts the run's own precedence rule (`q2-class-index.md` line 1: card labels are to be re-classified *with* the D3 rubric when they conflict). Either restore PARTIAL, or keep NOT and justify it against D3's own PARTIAL wording ("item/action-level retention scores or surrogate linkage") — the substantive argument already in the G4 row ("item-wise RL advantage exists, but only inside policy learning") is the right basis; drop the appeal to the card header. Note the count consequence: D3's headline "3 partial per-touch retention-credit hits" no longer matches the deliverables' PARTIAL set.

4. **`literature-review.md` §1 and §2 headings (~lines 25, 33) assert per-interaction retention credit for papers the body classifies NOT.** §1 "Retention credited to individual interactions — reciprocal / two-sided platforms" contains only MRet, which the body correctly calls NOT attribution under D3. §2 under the same phrase contains RLUR, AURO, SEC, LRF, and Downstream Rewards, all NOT. A reader scanning headings gets the opposite of the Q2 answer. Retitle to something neutral (e.g. "Retention-targeted methods — reciprocal / two-sided platforms") so the heading does not carry a classification the body denies.

5. **`executive-summary.md`, "Direct answer to Q2" (~line 18) — omits the dating-vertical negative result.** D3 §F and §H.3 establish, with a logged query trail, that Tinder/Hinge/Bumble/Match Group have published **zero** swipe→days-active attribution work, and that the closest dating literature optimizes matches or ecosystem retention. That is the most decision-relevant Q2 finding for this product and it appears nowhere in the Q2 answer. Add one sentence citing D3 §F. While editing: the same paragraph lists RevisitMTL, TOIS IURO+, and Netflix as PARTIAL but omits Ding et al., *Interpretable User Retention Modeling in Recommendation*, RecSys 2023, which the table below it marks PARTIAL at G13.

6. **`method-tracker.md` — four quantitative claims and one method name have no source in the compact summaries or D3.** "up to 100× evaluation velocity" (Airbnb row), "Millions of users" (Vlontzos/Spotify row), "2% randomized boost" (Meta producer-HTE row), and "Three years of retail data" (IMA row) appear in neither the cards nor D3; D3 supports only the Spotify "−7% impressions" figure. The label "Deep Twin" (Vlontzos rows in both `method-tracker.md` and `executive-summary.md` §Most Fundamental Methods #5) is likewise unsourced, though the underlying separate treated/organic modeling is supported by the card. This matters beyond bookkeeping because the Airbnb and Vlontzos \(P=3\) deployment scores rest on the unsupported numbers, and Vlontzos is rank 5 and is the cited basis for the holdback design in v1, v2, and v3. Either source them or mark them "not reported in card" as the file already does elsewhere.

7. **`method-tracker.md`, "Message holdback" row (~line 29) — a stub is scored I=3/B=2 for a composite of 7.** The row correctly withholds the number ("Stub; measured result not available"), but the per-touch-credit and bias-device scores are derived entirely from D3 prose about a source that `compact-summaries-1` records as unretrievable ("NotebookLM could not ingest the Medium URL; jina.ai and direct WebFetch also failed"). `literature-review.md` (~line 100) states the opposing rule: no discovery one-liner is treated as a measured result. Mark the row provisional/unscored so it does not sit at parity with verified deployed systems.

FILES READ: 9 of 9
